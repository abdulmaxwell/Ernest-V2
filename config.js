import dotenv from "dotenv";
dotenv.config();

export const ownerNumber = process.env.OWNER_NUMBER || "254793859108"; // fallback if not set in .env
export const caption = "🔥 Powered by Ernest Tech House"; // just in case you forgot this too
export const ownerNumbers = "254793859108"; // Without @s.whatsapp.net
